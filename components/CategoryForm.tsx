"use client";import{useState}from"react";export default function CategoryForm(){const[n,setN]=useState("");return <form className="panel mt-6 flex gap-2 max-w-xl" onSubmit={async e=>{e.preventDefault();const r=await fetch("/api/admin/categories",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:n})});if(r.ok)location.reload();else alert((await r.json()).error)}}><input className="input" placeholder="Category" value={n} onChange={e=>setN(e.target.value)}/><button className="btn btn-dark">ADD</button></form>}
