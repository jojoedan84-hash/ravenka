"use client";export default function DeleteProduct({id}:{id:string}){return <button className="btn" onClick={async()=>{if(!confirm("Hapus produk ini?"))return;const r=await fetch("/api/admin/products",{method:"DELETE",headers:{"content-type":"application/json"},body:JSON.stringify({id})});if(r.ok)location.href="/admin/products";else alert((await r.json()).error)}}>DELETE PRODUCT</button>}
