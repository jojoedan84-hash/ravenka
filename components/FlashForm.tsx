"use client";import{useState}from"react";export default function FlashForm({products}:{products:any[]}){const[f,s]=useState({name:"",startsAt:"",endsAt:"",productId:products[0]?.id||"",price:0,stockLimit:""});const set=(k:string,v:any)=>s({...f,[k]:v});return <form className="panel mt-6 grid md:grid-cols-3 gap-2" onSubmit={async e=>{e.preventDefault();const r=await fetch("/api/admin/flash-sales",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});if(r.ok)location.reload();else alert((await r.json()).error)}}><input className="input" placeholder="Name" value={f.name} onChange={e=>set("name",e.target.value)}/><input className="input" type="datetime-local" value={f.startsAt} onChange={e=>set("startsAt",e.target.value)}/><input className="input" type="datetime-local" value={f.endsAt} onChange={e=>set("endsAt",e.target.value)}/><select className="select" value={f.productId} onChange={e=>set("productId",e.target.value)}>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input className="input" type="number" placeholder="Sale price" value={f.price} onChange={e=>set("price",+e.target.value)}/><input className="input" type="number" placeholder="Stock limit" value={f.stockLimit} onChange={e=>set("stockLimit",+e.target.value)}/><button className="btn btn-dark md:col-span-3">CREATE FLASH SALE</button></form>}
