"use client";import{useState}from"react";export default function OrderStatus({id,value}:{id:string;value:string}){const[s,set]=useState(value);return <select className="select" value={s} onChange={async e=>{set(e.target.value);await fetch("/api/admin/orders/status",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:e.target.value})})}}>{["PENDING","PAID","PROCESSING","SHIPPED","COMPLETED","CANCELLED","EXPIRED"].map(x=><option key={x}>{x}</option>)}</select>}
