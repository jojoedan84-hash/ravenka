import{SignJWT,jwtVerify}from"jose";import{cookies}from"next/headers";const secret=new TextEncoder().encode(process.env.AUTH_SECRET||"change-me");export async function login(email:string){const t=await new SignJWT({role:"admin",email}).setProtectedHeader({alg:"HS256"}).setIssuedAt().setExpirationTime("7d").sign(secret);(await cookies()).set("ravenka_admin",t,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:604800})}export async function isAdmin(){try{const t=(await cookies()).get("ravenka_admin")?.value;if(!t)return false;return (await jwtVerify(t,secret)).payload.role==="admin"}catch{return false}}
