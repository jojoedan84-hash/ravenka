import{redirect}from"next/navigation";import{isAdmin}from"@/lib/auth";import{prisma}from"@/lib/prisma";import AdminNav from"@/components/AdminNav";import CategoryForm from"@/components/CategoryForm";export default async function Categories(){if(!await isAdmin())redirect("/admin/login");const c=await prisma.category.findMany({include:{_count:{select:{products:true}}},orderBy:{sortOrder:"asc"}});return <div className="admin-shell"><AdminNav/><main className="admin-main"><h1 className="text-4xl font-black">Categories</h1><CategoryForm/><div className="panel mt-6"><table><thead><tr><th>Name</th><th>Slug</th><th>Products</th></tr></thead><tbody>{c.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.slug}</td><td>{x._count.products}</td></tr>)}</tbody></table></div></main></div>}
