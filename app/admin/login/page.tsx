"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function Login(){const[e,setE]=useState("");const[p,setP]=useState("");const[r,setR]=useState(false);const router=useRouter();return <main className="min-h-screen grid place-items-center bg-neutral-100"><form className="panel w-full max-w-md" onSubmit={async x=>{x.preventDefault();const q=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:e,password:p})});if(q.ok)router.push("/admin");else setR(true)}}><p className="text-xs tracking-[.3em] font-bold">RAVENKA ADMIN</p><h1 className="text-4xl font-black mt-2">SIGN IN</h1><input className="input mt-6" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="input mt-2" type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/>{r&&<p className="text-red-600 mt-2">Login gagal</p>}<button className="btn btn-dark w-full mt-5">LOGIN</button></form></main>}
