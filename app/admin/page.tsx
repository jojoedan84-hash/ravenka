import{redirect}from"next/navigation";import{isAdmin}from"@/lib/auth";import{prisma}from"@/lib/prisma";import AdminNav from"@/components/AdminNav";import{rupiah}from"@/lib/utils";export default async function Admin(){if(!await isAdmin())redirect("/admin/login");const[a,b,c]=await Promise.all([prisma.product.count(),prisma.order.count(),prisma.order.aggregate({where:{status:"PAID"},_sum:{total:true}})]);return <div className="admin-shell"><AdminNav/><main className="admin-main"><p className="text-xs tracking-[.3em] font-bold">OVERVIEW</p><h1 className="text-4xl font-black mt-2">Dashboard</h1><div className="grid md:grid-cols-3 gap-4 mt-8"><div className="stat"><small>Products</small><b className="text-3xl block">{a}</b></div><div className="stat"><small>Orders</small><b className="text-3xl block">{b}</b></div><div className="stat"><small>Paid Revenue</small><b className="text-xl block mt-2">{rupiah(c._sum.total||0)}</b></div></div></main></div>}
