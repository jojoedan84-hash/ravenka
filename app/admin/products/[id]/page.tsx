import{redirect,notFound}from"next/navigation";import{isAdmin}from"@/lib/auth";import{prisma}from"@/lib/prisma";import AdminNav from"@/components/AdminNav";import ProductForm from"@/components/ProductForm";import DeleteProduct from"@/components/DeleteProduct";export default async function Edit({params}:{params:Promise<{id:string}>}){if(!await isAdmin())redirect("/admin/login");const{id}=await params;const[p,c]=await Promise.all([prisma.product.findUnique({where:{id},include:{variants:true}}),prisma.category.findMany()]);if(!p)notFound();return <div className="admin-shell"><AdminNav/><main className="admin-main"><h1 className="text-4xl font-black">Edit Product</h1><ProductForm categories={c} initial={p}/><div className="mt-3"><DeleteProduct id={p.id}/></div></main></div>}
