import{redirect}from"next/navigation";import{isAdmin}from"@/lib/auth";import{prisma}from"@/lib/prisma";import AdminNav from"@/components/AdminNav";import FlashForm from"@/components/FlashForm";export default async function F(){if(!await isAdmin())redirect("/admin/login");const[f,p]=await Promise.all([prisma.flashSale.findMany({include:{items:{include:{product:true}}}}),prisma.product.findMany({where:{active:true}})]);return <div className="admin-shell"><AdminNav/><main className="admin-main"><h1 className="text-4xl font-black">Flash Sale</h1><FlashForm products={p}/><div className="grid gap-4 mt-6">{f.map(x=><div className="panel" key={x.id}><b>{x.name}</b><p>{x.startsAt.toLocaleString("id-ID")} — {x.endsAt.toLocaleString("id-ID")}</p>{x.items.map(i=><div key={i.id}>{i.product.name} — Rp {i.price.toLocaleString("id-ID")}</div>)}</div>)}</div></main></div>}
