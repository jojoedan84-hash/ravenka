import{NextResponse}from"next/server";import{isAdmin}from"@/lib/auth";import{prisma}from"@/lib/prisma";export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();return NextResponse.json(await prisma.flashSale.create({data:{name:b.name,startsAt:new Date(b.startsAt),endsAt:new Date(b.endsAt),items:{create:{productId:b.productId,price:+b.price,stockLimit:b.stockLimit?+b.stockLimit:null}}}}))}
