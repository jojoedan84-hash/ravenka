import{NextResponse}from"next/server";import{isAdmin}from"@/lib/auth";import{prisma}from"@/lib/prisma";export async function POST(req:Request){if(!await isAdmin())return NextResponse.json({error:"Unauthorized"},{status:401});try{const b=await req.json();return NextResponse.json(await prisma.voucher.create({data:{code:b.code.toUpperCase(),type:b.type,value:+b.value,minPurchase:+b.minPurchase||0,maxDiscount:b.maxDiscount?+b.maxDiscount:null,quota:b.quota?+b.quota:null,startsAt:new Date(b.startsAt),endsAt:new Date(b.endsAt)}}))}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
